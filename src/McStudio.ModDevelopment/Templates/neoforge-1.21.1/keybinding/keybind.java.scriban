package {{ package }}.init;

import net.minecraft.client.KeyMapping;
import net.neoforged.neoforge.client.event.RegisterKeyMappingsEvent;
import net.neoforged.neoforge.common.util.Lazy;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.fml.common.EventBusSubscriber;
import net.neoforged.api.distmarker.OnlyIn;
import net.neoforged.api.distmarker.Dist;
import com.mojang.blaze3d.platform.InputConstants;

@OnlyIn(Dist.CLIENT)
@EventBusSubscriber(modid = {{ JavaModName }}.MODID, value = Dist.CLIENT)
public class {{ JavaModName }}KeyMappings {

    {{ for keybinding in keybindings }}
    public static final Lazy<KeyMapping> {{ keybinding.field }} = Lazy.of(() -> new KeyMapping(
        "key.{{ modid }}.{{ keybinding.name }}",
        InputConstants.Type.KEYSYM,
        {{ keybinding.key_code }},
        "key.categories.{{ modid }}"
    ));
    {{ end }}

    @SubscribeEvent
    public static void registerKeyMappings(RegisterKeyMappingsEvent event) {
        {{ for keybinding in keybindings }}
        event.register({{ keybinding.field }}.get());
        {{ end }}
    }

    // Start of user code block keybind methods
    // End of user code block keybind methods
}