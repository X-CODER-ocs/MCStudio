package {{ package }}.init;

import net.neoforged.neoforge.client.event.EntityRenderersEvent;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.fml.common.EventBusSubscriber;
import net.neoforged.api.distmarker.OnlyIn;
import net.neoforged.api.distmarker.Dist;

@OnlyIn(Dist.CLIENT)
@EventBusSubscriber(modid = {{ JavaModName }}.MODID, value = Dist.CLIENT)
public class {{ JavaModName }}EntityRenderers {
    @SubscribeEvent
    public static void registerEntityRenderers(EntityRenderersEvent.RegisterRenderers event) {
        {{ for renderer in entity_renderers }}
        event.registerEntityRenderer({{ JavaModName }}Entities.{{ renderer.field }}.get(), {{ renderer.renderer_class }}::new);
        {{ end }}
    }
}